#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
typedef long long int lli;
int main()
{
    int noe, val;
    cin >> noe;
    vector<lli> vect;
    for (lli i = 0; i < noe; i++)
    {
        cin >> val;
        vect.push_back(val);
    }
    //code
    return 0;
}